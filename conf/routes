# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET	/	controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET	/assets/*file	controllers.Assets.at(path="/public", file)
GET	/tasks	controllers.Application.tasks
POST	/tasks	controllers.Application.create
POST	/tasks/:id/delete	controllers.Application.delete(id:Long)
POST /tasks:id/edit controllers.Application.edit(id:Long)
POST /edit:id/update controllers.Application.update(id:Long)

